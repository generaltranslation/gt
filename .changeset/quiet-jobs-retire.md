---
'@generaltranslation/api': minor
'gt': patch
'gt-sanity': patch
---

Expose successful job-poll responses through an `onPoll` hook. Route CLI font and wizard requests through the generated SDK, use the shared sequential job poller, and remove the retired hand-written HTTP client implementations.
