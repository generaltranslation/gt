---
'generaltranslation': patch
'@generaltranslation/api': patch
---

Accept whole markdown and MDX documents on the runtime translate endpoint via the new `fileFormat` entry metadata (`MD` or `MDX`).
