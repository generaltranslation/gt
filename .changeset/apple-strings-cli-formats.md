---
'gt': minor
'generaltranslation': patch
---

Register Apple `.strings`, `.stringsdict`, and `.xcstrings` catalogs as configurable CLI file types, upload `.strings` and `.stringsdict` sources for translation, and accept the new formats in the core file-format transform allowlist.
