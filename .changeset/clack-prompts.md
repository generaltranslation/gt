---
'gt': patch
---

Replace the custom Ink prompt UI with Clack, preserving searchable locale selection, custom aliases, defaults and cancellation. Keep unmatched locale searches open until a valid option is selected, and remove the CLI's Ink/React rendering dependencies.
