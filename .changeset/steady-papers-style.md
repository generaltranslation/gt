---
'@generaltranslation/compiler': patch
'gt': patch
---

Keep CSS, styled-jsx, script, title, and textarea payloads outside automatically inserted JSX translation regions, preserving valid raw-text content and the original style parent while translating surrounding text independently. Recognize Next.js optimized GT imports during compiler auto-insertion so manual translation boundaries remain intact after barrel optimization.

Apply compiler auto-insertion to React JavaScript generated by custom resource loaders while leaving other compiler transformations scoped to their existing JavaScript and TypeScript file extensions.
