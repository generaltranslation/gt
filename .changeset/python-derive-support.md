---
'@generaltranslation/python-extractor': patch
---

Add support for `derive()` as the primary function name (replacing `declare_static()`). `declare_static()` remains supported for backwards compatibility.
