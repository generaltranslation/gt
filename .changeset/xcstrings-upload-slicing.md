---
'gt': minor
---

Upload Apple `.xcstrings` catalogs: the source-language slice uploads as the source document, and per-locale slices are extracted from the shared catalog for translation uploads.
