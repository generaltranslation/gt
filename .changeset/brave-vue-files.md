---
'@generaltranslation/vue-extractor': minor
'gt': minor
---

Add a standalone Vue source extractor and integrate context-only string and rich-content extraction into the CLI. Extraction supports Vue SFC templates, Vue JSX and TSX, statically traceable local ESM reexports, translator forwarding through local callbacks, and the active consumer Vue compiler semantics.
