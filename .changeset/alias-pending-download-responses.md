---
'generaltranslation': patch
---

Restore configured locale aliases in the shared API adapter's `downloadFileBatch()` pending file entries, matching the locale identities already returned for completed files.
