---
"gt-format": minor
"generaltranslation": patch
"gt-i18n": patch
---

Extract locale and formatting primitives into the new `gt-format` package and update `generaltranslation/core` to re-export the shared helpers.
