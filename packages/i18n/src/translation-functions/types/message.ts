/**
 * Valid types for messages that can be registered
 */
export type RegisterableMessage = string | string[];

/**
 * Valid types for messages that can be interpolated
 */
export type InterpolatableMessage = string | null | undefined;
