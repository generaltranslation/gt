import { SupportedLibraries } from '../../types';
export declare function translateJson(sourceJson: any, defaultLocale: string, locales: string[], library: SupportedLibraries, apiKey: string, projectId: string, config: string, translationsDir: string): Promise<void>;
