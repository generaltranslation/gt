import { Updates } from '../types';
export default function saveTranslations(baseUrl: string, apiKey: string, versionId: string, translationsDir: string): Promise<void>;
export declare function saveSourceFile(filepath: string, data: Updates): void;
