# GT credentials. Copy this file to .env and fill in the values from your GT
# dashboard to inline them into the bundle. In this plain-Rollup setup the
# committed src/_gt/*.json files are what renders, so these can stay empty.
GT_PROJECT_ID=
GT_DEV_API_KEY=
