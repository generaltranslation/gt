# Optional. Only needed for gt-react development hot reload (see the README).
# Copy this file to .env.local and fill in your own values. A development key
# starts with gtx-dev-. Never commit real credentials, and never put a
# production key (gtx-api-) in browser code.
VITE_GT_PROJECT_ID=
VITE_GT_DEV_API_KEY=
