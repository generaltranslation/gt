---
title: LocaleProperties
description: TypeScript interface containing comprehensive locale information
---

## Overview

`LocaleProperties` is a comprehensive interface that provides detailed linguistic and regional information about a locale. It includes display names, native names, region/script details, and emoji representations for any valid BCP-47 locale code.

```typescript
interface LocaleProperties {
  code: string;
  name: string;
  nativeName: string;
  languageCode: string;
  languageName: string;
  nativeLanguageName: string;
  nameWithRegionCode: string;
  nativeNameWithRegionCode: string;
  regionCode: string;
  regionName: string;
  nativeRegionName: string;
  scriptCode: string;
  scriptName: string;
  nativeScriptName: string;
  maximizedCode: string;
  maximizedName: string;
  nativeMaximizedName: string;
  minimizedCode: string;
  minimizedName: string;
  nativeMinimizedName: string;
  emoji: string;
}
```

This interface is returned by `getLocaleProperties()` methods and provides everything needed for rich locale-aware user interfaces.

<Callout>
**Maximization & Minimization:**
The interface includes both maximized (with likely script/region) and minimized (shortest valid) locale codes, following Unicode CLDR standards for locale processing.
</Callout>

---

## Properties

### Basic Locale Information

#### `code: string`
The original locale code that was queried.

```typescript
const props = gt.getLocaleProperties('de-AT');
console.log(props.code); // "de-AT"
```

#### `name: string`
Localized display name in the default/source language.

```typescript
const props = gt.getLocaleProperties('ja-JP');
console.log(props.name); // "Japanese (Japan)" (in English)
```

#### `nativeName: string`
Display name in the locale's own language.

```typescript
const props = gt.getLocaleProperties('ja-JP');
console.log(props.nativeName); // "日本語 (日本)"
```

---

### Language Information

#### `languageCode: string`
The base language code without region or script.

```typescript
const props = gt.getLocaleProperties('zh-Hans-CN');
console.log(props.languageCode); // "zh"
```

#### `languageName: string`
Language name in the default/source language.

```typescript
const props = gt.getLocaleProperties('de-AT');
console.log(props.languageName); // "German"
```

#### `nativeLanguageName: string`
Language name in the language's own script.

```typescript
const props = gt.getLocaleProperties('de-AT');
console.log(props.nativeLanguageName); // "Deutsch"
```

---

### Region-Aware Names

#### `nameWithRegionCode: string`
Display name with region code in parentheses.

```typescript
const props = gt.getLocaleProperties('fr-CA');
console.log(props.nameWithRegionCode); // "French (CA)"
```

#### `nativeNameWithRegionCode: string`
Native display name with region code.

```typescript
const props = gt.getLocaleProperties('fr-CA');
console.log(props.nativeNameWithRegionCode); // "français (CA)"
```

---

### Regional Information

#### `regionCode: string`
ISO 3166-1 alpha-2 region code or UN M.49 numeric code.

```typescript
const props = gt.getLocaleProperties('en-GB');
console.log(props.regionCode); // "GB"
```

#### `regionName: string`
Region name in the default/source language.

```typescript
const props = gt.getLocaleProperties('en-GB');
console.log(props.regionName); // "United Kingdom"
```

#### `nativeRegionName: string`
Region name in the locale's language.

```typescript
const props = gt.getLocaleProperties('de-AT');
console.log(props.nativeRegionName); // "Österreich"
```

---

### Script Information

#### `scriptCode: string`
ISO 15924 script code (4-letter code).

```typescript
const props = gt.getLocaleProperties('zh-Hans-CN');
console.log(props.scriptCode); // "Hans"
```

#### `scriptName: string`
Script name in the default/source language.

```typescript
const props = gt.getLocaleProperties('zh-Hans-CN');
console.log(props.scriptName); // "Simplified"
```

#### `nativeScriptName: string`
Script name in the locale's language.

```typescript
const props = gt.getLocaleProperties('zh-Hans-CN');
console.log(props.nativeScriptName); // "简体"
```

---

### Maximized Locale Information

#### `maximizedCode: string`
Locale code with likely script and region added.

```typescript
const props = gt.getLocaleProperties('zh');
console.log(props.maximizedCode); // "zh-Hans-CN"
```

#### `maximizedName: string`
Full display name including likely script.

```typescript
const props = gt.getLocaleProperties('zh');
console.log(props.maximizedName); // "Chinese (Simplified, China)"
```

#### `nativeMaximizedName: string`
Full native display name with script and region.

```typescript
const props = gt.getLocaleProperties('zh');
console.log(props.nativeMaximizedName); // "中文（简体，中国）"
```

---

### Minimized Locale Information

#### `minimizedCode: string`
Shortest valid locale code (removes redundant components).

```typescript
const props = gt.getLocaleProperties('de-Latn-DE');
console.log(props.minimizedCode); // "de"
```

#### `minimizedName: string`
Display name for the minimized locale.

```typescript
const props = gt.getLocaleProperties('en-Latn-US');
console.log(props.minimizedName); // "English"
```

#### `nativeMinimizedName: string`
Native display name for the minimized locale.

```typescript
const props = gt.getLocaleProperties('de-Latn-DE');
console.log(props.nativeMinimizedName); // "Deutsch"
```

---

### Visual Representation

#### `emoji: string`
Flag or symbol emoji representing the locale's region.

```typescript
const props = gt.getLocaleProperties('fr-CA');
console.log(props.emoji); // "🇨🇦"
```

---

## Usage Examples

### Complete Locale Information Display

```typescript
const gt = new GT({ sourceLocale: 'en-US' });
const props = gt.getLocaleProperties('de-AT');

console.log(`
Locale: ${props.code}
Display Name: ${props.name}
Native Name: ${props.nativeName}
Language: ${props.languageName} (${props.languageCode})
Region: ${props.regionName} (${props.regionCode})
Script: ${props.scriptName} (${props.scriptCode})
Minimized: ${props.minimizedCode}
Maximized: ${props.maximizedCode}
Emoji: ${props.emoji}
`);

// Output:
// Locale: de-AT
// Display Name: Austrian German
// Native Name: Österreichisches Deutsch
// Language: German (de)
// Region: Austria (AT)
// Script: Latin (Latn)
// Minimized: de-AT
// Maximized: de-Latn-AT
// Emoji: 🇦🇹
```

### Building Rich Locale Selector

```typescript
interface LocaleOption {
  code: string;
  displayName: string;
  nativeName: string;
  flag: string;
  region: string;
  isRTL: boolean;
}

function createRichLocaleSelector(
  gt: GT,
  locales: string[]
): LocaleOption[] {
  return locales.map(locale => {
    const props = gt.getLocaleProperties(locale);
    
    return {
      code: locale,
      displayName: props.name,
      nativeName: props.nativeName,
      flag: props.emoji,
      region: props.regionName,
      isRTL: gt.getLocaleDirection(locale) === 'rtl'
    };
  });
}

const options = createRichLocaleSelector(gt, [
  'ar-SA',  // Arabic (Saudi Arabia)
  'en-US',  // English (United States)
  'zh-CN',  // Chinese (China)
  'he-IL'   // Hebrew (Israel)
]);

// Results include RTL information, flags, and rich naming
```

### Locale Hierarchy Visualization

```typescript
interface LocaleHierarchy {
  language: {
    code: string;
    name: string;
    nativeName: string;
  };
  regions: Array<{
    code: string;
    name: string;
    nativeName: string;
    emoji: string;
  }>;
}

function createLocaleHierarchy(
  gt: GT,
  locales: string[]
): LocaleHierarchy[] {
  const grouped = new Map<string, LocaleProperties[]>();
  
  // Group by language code
  locales.forEach(locale => {
    const props = gt.getLocaleProperties(locale);
    const langCode = props.languageCode;
    
    if (!grouped.has(langCode)) {
      grouped.set(langCode, []);
    }
    grouped.get(langCode)!.push(props);
  });
  
  return Array.from(grouped.entries()).map(([langCode, regionProps]) => ({
    language: {
      code: langCode,
      name: regionProps[0].languageName,
      nativeName: regionProps[0].nativeLanguageName
    },
    regions: regionProps.map(props => ({
      code: props.regionCode,
      name: props.regionName,
      nativeName: props.nativeRegionName,
      emoji: props.emoji
    }))
  }));
}
```

---

## Advanced Usage Patterns

### Locale Comparison Tool

```typescript
function compareLocales(gt: GT, locale1: string, locale2: string) {
  const props1 = gt.getLocaleProperties(locale1);
  const props2 = gt.getLocaleProperties(locale2);
  
  return {
    sameLanguage: props1.languageCode === props2.languageCode,
    sameScript: props1.scriptCode === props2.scriptCode,
    sameRegion: props1.regionCode === props2.regionCode,
    similarity: calculateSimilarity(props1, props2),
    comparison: {
      [locale1]: {
        display: props1.name,
        native: props1.nativeName,
        minimized: props1.minimizedCode
      },
      [locale2]: {
        display: props2.name,  
        native: props2.nativeName,
        minimized: props2.minimizedCode
      }
    }
  };
}

function calculateSimilarity(props1: LocaleProperties, props2: LocaleProperties): number {
  let score = 0;
  if (props1.languageCode === props2.languageCode) score += 50;
  if (props1.scriptCode === props2.scriptCode) score += 30;
  if (props1.regionCode === props2.regionCode) score += 20;
  return score;
}

const comparison = compareLocales(gt, 'en-US', 'en-GB');
// { sameLanguage: true, sameScript: true, sameRegion: false, similarity: 80, ... }
```

### Smart Locale Fallback

```typescript
function findBestFallback(
  gt: GT,
  requestedLocale: string,
  availableLocales: string[]
): { locale: string; score: number; reason: string } {
  const requestedProps = gt.getLocaleProperties(requestedLocale);
  
  const candidates = availableLocales.map(locale => {
    const props = gt.getLocaleProperties(locale);
    let score = 0;
    let reasons: string[] = [];
    
    if (props.languageCode === requestedProps.languageCode) {
      score += 50;
      reasons.push('same language');
    }
    
    if (props.scriptCode === requestedProps.scriptCode) {
      score += 30;
      reasons.push('same script');
    }
    
    if (props.regionCode === requestedProps.regionCode) {
      score += 20;
      reasons.push('same region');
    }
    
    return {
      locale,
      score,
      reason: reasons.join(', ') || 'different language family'
    };
  });
  
  return candidates.sort((a, b) => b.score - a.score)[0];
}

const fallback = findBestFallback(gt, 'fr-CA', ['fr-FR', 'en-CA', 'es-ES']);
// { locale: 'fr-FR', score: 80, reason: 'same language, same script' }
```

---

## Related Types and Functions

- **[getLocaleProperties()](/docs/core/class/methods/locales/getLocaleProperties)** - Method that returns this interface
- **[getLocaleName()](/docs/core/class/methods/locales/getLocaleName)** - Returns just the display name
- **[getLocaleEmoji()](/docs/core/class/methods/locales/getLocaleEmoji)** - Returns just the emoji
- **[CustomMapping](/docs/core/types/CustomMapping)** - Interface for custom locale definitions

---

## Notes

- All names are localized according to the GT instance's source locale
- Script and region codes follow Unicode CLDR standards
- Maximization/minimization follows Unicode locale algorithms
- Native names use the locale's own writing system
- Emoji selection prioritizes region over language for visual representation
- Properties are computed dynamically and may vary based on Intl API availability

## Next Steps

- **[Use getLocaleProperties() method](/docs/core/class/methods/locales/getLocaleProperties)**
- **[Learn about locale validation](/docs/core/class/methods/locales/isValidLocale)**
- **[Explore custom mappings](/docs/core/types/CustomMapping)**
- **[Work with locale directions](/docs/core/class/methods/locales/getLocaleDirection)**